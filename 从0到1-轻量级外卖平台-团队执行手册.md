# 轻量级外卖平台从 0 到 1 团队执行手册

> 适用项目：SE_Practicum（轻量级外卖服务平台，仿饿了么）
> 适用人员：A、B、C 三人
> 课程基线：26271学期-软件工程综合实践.md
> 编写日期：2026-09-02
> 版本：V1.0

---

## 0. 先读这一页：这份手册怎么用

你们三人都是第一次做现代软件项目，所以不要把本手册当成“建议清单”，而要把它当成施工图：按顺序完成当前步骤，确认“完成标准”后再进入下一步。

本手册只规定项目执行方式，不要求你们手写所有代码。样板代码、语法排错和重复性代码可以借助 AI，但架构决策、业务规则、测试场景、验收结论必须由三人理解、讨论和确认。

每个功能必须形成小闭环：

```text
需求/用例
  ↓
接口契约
  ↓
A 先写测试（Red）
  ↓
运行测试并保存失败证据
  ↓
B 写最少业务代码（Green）
  ↓
A、B 重构（Refactor）
  ↓
C 按接口契约做前端
  ↓
三人联调、回归、记录
  ↓
提交 feature 分支并合并 develop
```

### 0.1 课程硬性要求

1. 全程测试优先开发（TDD）；
2. 核心接口覆盖率 100%，关键业务方法覆盖率至少 90%；
3. 使用前后端分离架构；
4. 后端按 Entity、Mapper/Dao、Service、Controller 分层；
5. API 使用 /api/v1，URI 尽量使用资源名词；
6. 需求变更必须新增测试并执行原有回归测试；
7. Git 提交历史必须能够看出“测试先于实现”；
8. 必须保留测试类、测试日志、覆盖率、缺陷、需求变更和开发日志；
9. 必须完成交叉验收；
10. 最终交付物必须能够由组员解释，而不是只会运行。

### 0.2 当前仓库真实状态

截至 2026-09-02，仓库已经有：

- backend：Java 17 + Spring Boot 4.1.1 + Spring Modulith 后端骨架；
- user、merchant、restaurant、item、shopping、order 模块骨架；
- 只测试上下文和模块边界的 BackendApplicationTests；
- frontend/README.md，但没有 Vue 工程；
- docs/ 下的需求、架构、数据库、API、测试和开发日志占位文档；
- 根目录课程要求文档和交接记录。

尚未完成：

- MyBatis、Flyway、数据库表和迁移脚本；
- JWT、统一响应、全局异常和参数校验；
- 真实 Controller、Service、Mapper；
- 任何真实 /api/v1 业务接口；
- 前端初始化、联调、端到端验收；
- 业务测试、覆盖率报告和交叉验收材料。

当前 Git 状态以实际命令输出为准。开始工作前，不要假设本地分支是最新的。

### 0.3 固定角色

用户最新指定的分工覆盖旧交接文档里的角色表，本手册按以下分工执行：

| 成员 | 固定职责                                                               | 不负责的事项                                        |
| ---- | ---------------------------------------------------------------------- | --------------------------------------------------- |
| A    | 后端架构、公共组件、TDD 测试、MockMvc、覆盖率、回归测试、最终发布      | 不独自决定业务规则，不把核心业务逻辑塞进 Controller |
| B    | 数据库设计、Flyway、MyBatis、Mapper、Service、业务规则、事务和后端实现 | 不跳过 A 的失败测试，不擅自修改接口契约             |
| C    | Vue 前端、页面、交互、Axios、Pinia、联调、验收文档和演示材料           | 不猜测接口字段，不用前端隐藏按钮代替后端权限        |

三人共同负责：需求讨论、技术约定、架构解释、需求变更分析、交叉验收和 AI 产出审核。

---

## 1. 已冻结的技术和业务约定

除非课程老师明确要求变更，否则开发中不能临时换方案。

### 1.1 技术栈

| 部分        | 方案                                            |
| ----------- | ----------------------------------------------- |
| Java        | OpenJDK 17                                      |
| 后端        | Spring Boot 4.1.1                               |
| 模块管理    | Spring Modulith 2.1.1                           |
| Web         | Spring MVC                                      |
| 持久化      | MyBatis                                         |
| 数据库迁移  | Flyway                                          |
| 数据库      | MySQL 8                                         |
| 认证        | JWT Bearer Token                                |
| 前端        | Vue 3 + Vite                                    |
| 前端路由    | Vue Router                                      |
| 前端状态    | Pinia                                           |
| UI          | Element Plus                                    |
| HTTP 客户端 | Axios                                           |
| 后端测试    | JUnit 5、Mockito、MockMvc、Spring Modulith Test |
| 前端测试    | Vitest                                          |
| 接口联调    | Postman 或 ApiFox，三人选一个并统一使用         |

### 1.2 业务约定

- 普通用户和商家共用 users 表，merchants 保存商家扩展资料；
- 登录返回身份类型和 JWT；
- 对外 API、数据库和前端统一使用 shop、product；
- 现有 Java 包 restaurant、item 先保留，通过公开 DTO/Facade 兼容，阶段 3 再做小范围迁移；
- 商家创建店铺后初始状态是 CLOSED；
- 店铺只有 OPEN 时允许创建订单；
- 商品状态使用 ON_SALE、OFF_SALE；
- 订单初始状态是 PENDING_PAYMENT，本版本没有真实支付；
- 只有 PENDING_PAYMENT 订单可以取消，取消是状态迁移，不删除订单；
- 第一阶段一个订单不能包含多个店铺商品；
- 价格由服务端读取并计算，不能信任客户端金额；
- 订单明细保存商品名称和成交单价快照；
- 订单创建、订单明细、库存扣减和购物车清理必须在一个事务内完成；
- 所有列表默认 page=1、pageSize=10，上限由统一配置控制；
- 金额使用 BigDecimal，时间统一使用 ISO-8601；
- 密码使用 BCrypt 等摘要保存，不能返回、日志输出或提交明文密码。

### 1.3 Git 约定

```text
feature/*  →  develop  →  main
```

- develop：日常集成分支；
- main：阶段验收通过后才合并的发布分支；
- 每项工作使用独立 feature 分支；
- 每个 PR 至少由另一名成员检查；
- A 负责阶段发布合并，但不能替代其他成员的代码审查。

### 1.4 固定分支清单

除 develop 和 main 外，按下面的名称创建任务分支。任务完成后合并到 develop，再删除远程 feature 分支：

| 负责人 | 分支                      |
| ------ | ------------------------- |
| A      | feature/a-architecture    |
| A      | feature/a-test-user       |
| A      | feature/a-test-shop       |
| A      | feature/a-test-product    |
| A      | feature/a-test-cart       |
| A      | feature/a-test-order      |
| B      | feature/b-database        |
| B      | feature/b-user            |
| B      | feature/b-shop            |
| B      | feature/b-product         |
| B      | feature/b-cart            |
| B      | feature/b-order           |
| C      | feature/c-frontend-init   |
| C      | feature/c-user-pages      |
| C      | feature/c-shop-pages      |
| C      | feature/c-cart-pages      |
| C      | feature/c-order-pages     |
| C      | feature/c-acceptance-docs |

如果同一成员需要同时处理两个任务，必须使用两个分支，不要把不相关修改混在一起。

### 1.5 三周固定排期

| 时间                   | 阶段目标              | A                              | B                             | C                    |
| ---------------------- | --------------------- | ------------------------------ | ----------------------------- | -------------------- |
| 2026-08-31～2026-09-02 | 基线、环境、技术冻结  | 公共架构和测试约定             | 数据库方案和连接              | 前端工具和项目说明   |
| 2026-09-03～2026-09-06 | 阶段 1 基础闭环       | 用户/商家/商品/购物车/订单测试 | 数据库、Mapper、Service、事务 | Vue 初始化和基础页面 |
| 2026-09-07～2026-09-13 | 阶段 2 需求变更       | 新测试、回归和覆盖率           | 变更实现和迁移                | 页面适配和联调       |
| 2026-09-14～2026-09-18 | 阶段 3 重构和验收准备 | 架构整理、缺陷回归             | SQL、事务和数据一致性         | 页面完善、截图、报告 |
| 2026-09-19～2026-09-20 | 发布交付              | 合并 main、打标签              | 最终数据库验证                | 交叉验收和交付材料   |

---

## 2. 第 0 天：三人共同安装和验证环境

### 2.1 所有人都做一次

1. 安装 Git、OpenJDK 17、IntelliJ IDEA、Node.js LTS、MySQL 8；
2. 登录 GitHub，确认三人都有仓库读写权限；
3. 克隆仓库，进入项目目录；
4. 更新 develop 并查看状态。

```powershell
git clone <你们的GitHub仓库地址>
cd SE_Practicum
git switch develop
git pull origin develop
git status --short --branch
```

完成标准：

- 当前分支是 develop；
- 能看到 origin/develop；
- 三人本地都使用 Java 17；
- 不会把 IDE 配置提交到仓库。

遇到问题时不要强制覆盖代码：先复制 git status 输出，再在群里说明具体错误。

### 2.2 A：验证后端和测试工具

```powershell
cd backend
.\mvnw.cmd test
```

A 在 IDEA 中确认：

- BackendApplication.java 可以运行；
- 服务默认监听 http://localhost:8080；
- src/test/java 可以运行；
- JUnit 5、MockMvc 和 Spring Modulith Test 可用。

A 将日期、命令、结果写入 docs/dev-log/README.md。

### 2.3 B：验证 MySQL 和数据库权限（逐步操作）

> 本节只由 B 负责执行，A 和 C 在最后做连接和安全复核。目标是：本机有可用的 MySQL 8 服务、开发库、测试库和项目专用账号，并能证明应用账号只能访问项目数据库。

#### 2.3.1 确认目录和 Git 状态

B 打开 PowerShell：

```powershell
cd D:\Projects\SchoolWorks\SW_2609\SE_Practicum
git status --short --branch
```

这里只检查，不修改代码。看到其他成员未提交修改时，不执行清理、重置或强制覆盖命令。

#### 2.3.2 检查 MySQL 客户端

```powershell
mysql --version
where.exe mysql
```

应看到 MySQL 8.x。如果提示找不到 mysql：从开始菜单打开 MySQL 8.0 Command Line Client；记录完整错误发给 A；不要下载来路不明的 mysql.exe。

#### 2.3.3 检查 Windows 服务

```powershell
Get-Service | Where-Object { $_.Name -like '*MySQL*' -or $_.DisplayName -like '*MySQL*' } | Format-Table Status,Name,DisplayName
```

常见服务名是 MySQL80。若状态为 Stopped，使用管理员 PowerShell：

```powershell
Start-Service -Name MySQL80
Get-Service -Name MySQL80
```

成功标准是 Status 为 Running。若实际服务名不是 MySQL80，使用查询结果中的真实 Name。检查 3306 端口：

```powershell
Get-NetTCPConnection -LocalPort 3306 -ErrorAction SilentlyContinue
```

没有监听结果时，先解决 MySQL 服务问题，不要开始配置 Spring Boot。

#### 2.3.4 登录管理员账号

不要把密码写在命令中：

```powershell
mysql -u root -p -h 127.0.0.1 -P 3306
```

出现 Enter password: 后再输入密码。登录后执行：

```sql
SELECT VERSION();
SELECT USER(), CURRENT_USER();
SHOW DATABASES;
```

如果 Access denied，依次确认用户名、密码、Host、端口和 MySQL 服务状态。不要连续猜密码，也不要把密码发到群里。

#### 2.3.5 创建开发库和测试库

在 mysql> 提示符中逐条执行：

```sql
CREATE DATABASE IF NOT EXISTS delivery_dev CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS delivery_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES LIKE 'delivery%';
```

必须看到 delivery_dev 和 delivery_test。开发库用于日常联调和演示；测试库用于自动化测试、事务回滚和测试数据。测试程序不能连接 delivery_dev。

#### 2.3.6 创建项目专用账号

不要让 Spring Boot 使用 root。下面的密码是占位符，必须替换为只保存在 B 本机的强密码：

```sql
CREATE USER IF NOT EXISTS 'delivery_app'@'localhost' IDENTIFIED BY '@2006devinchang';
CREATE USER IF NOT EXISTS 'delivery_app'@'127.0.0.1' IDENTIFIED BY '@2006devinchang';
GRANT ALL PRIVILEGES ON delivery_dev.* TO 'delivery_app'@'localhost';
GRANT ALL PRIVILEGES ON delivery_test.* TO 'delivery_app'@'localhost';
GRANT ALL PRIVILEGES ON delivery_dev.* TO 'delivery_app'@'127.0.0.1';
GRANT ALL PRIVILEGES ON delivery_test.* TO 'delivery_app'@'127.0.0.1';
FLUSH PRIVILEGES;
```

密码至少 12 位，包含大小写字母、数字和符号；不得写入仓库、截图、日志或 AI 对话。

#### 2.3.7 检查权限和连接

管理员连接执行：

```sql
SHOW GRANTS FOR 'delivery_app'@'localhost';
SHOW GRANTS FOR 'delivery_app'@'127.0.0.1';
```

权限应只涉及 delivery_dev.* 和 delivery_test.*，不应出现全局所有数据库权限。退出管理员连接后，用项目账号分别连接两个库：

```powershell
mysql -u delivery_app -p -h 127.0.0.1 -P 3306 delivery_dev
mysql -u delivery_app -p -h 127.0.0.1 -P 3306 delivery_test
```

每个连接执行 SELECT DATABASE(), CURRENT_USER();，并创建、删除名为 permission_check 的临时表。成功标准是能建表、删表且没有权限错误。

#### 2.3.8 配置本机环境变量

```powershell
$env:DELIVERY_DB_URL = "jdbc:mysql://127.0.0.1:3306/delivery_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai"
$env:DELIVERY_DB_USERNAME = "delivery_app"
$env:DELIVERY_DB_PASSWORD = "<本机密码>"
$env:DELIVERY_JWT_SECRET = "<本机生成的随机长字符串>"
```

变量只对当前 PowerShell 窗口有效。IDEA 启动后端时，在 Run Configuration 的 Environment variables 中设置同名变量。不要打印密码。

#### 2.3.9 命令行最终测试

```powershell
mysql -u delivery_app -p -h 127.0.0.1 -P 3306 -e "SELECT DATABASE(), CURRENT_USER();" delivery_dev
mysql -u delivery_app -p -h 127.0.0.1 -P 3306 -e "SELECT DATABASE(), CURRENT_USER();" delivery_test
```

两条命令均成功后，B 才开始 Flyway 和 Spring Boot 数据源配置。

#### 2.3.10 IDEA Database 工具

在 IDEA 中新增两个 MySQL 连接：Host 127.0.0.1、Port 3306、User delivery_app，分别选择 delivery_dev 和 delivery_test。密码只保存在本机。不要在 IDEA 中随意改业务表结构，所有结构变化必须由 Flyway 脚本管理。

#### 2.3.11 Spring Boot 配置前检查

application.properties 只使用环境变量：

```properties
spring.datasource.url=${DELIVERY_DB_URL}
spring.datasource.username=${DELIVERY_DB_USERNAME}
spring.datasource.password=${DELIVERY_DB_PASSWORD}
```

测试配置必须指向 delivery_test；开发和测试不能混库；真实密码不能出现在仓库。

#### 2.3.12 交接和提交

B 发给 A、C：

```text
【MySQL 环境已完成】
负责人：B
MySQL 版本：8.x
开发数据库：delivery_dev
测试数据库：delivery_test
应用账号：delivery_app
已验证：命令行连接、两个数据库权限、临时建表和删除
未共享：数据库密码、JWT 密钥
下一步：配置 Flyway 并编写 V1 迁移
```

只提交不含敏感值的说明：

```powershell
git switch develop
git pull --ff-only origin develop
git switch -c feature/b-database
git add docs/database docs/dev-log
git commit -m "docs(database): record local mysql setup"
git push -u origin feature/b-database
```

#### 2.3.13 常见错误

| 错误                       | 含义                         | 处理                              |
| -------------------------- | ---------------------------- | --------------------------------- |
| mysql 找不到               | 客户端未加入 PATH            | 使用完整路径或修复 PATH，重开终端 |
| Can't connect              | 服务未启动、端口或 Host 错误 | 检查服务、3306 和 127.0.0.1       |
| Access denied              | 用户、密码或 Host 授权不匹配 | 分别检查 localhost 和 127.0.0.1   |
| Unknown database           | 数据库未创建或名称拼错       | 管理员执行 SHOW DATABASES         |
| 应用连接 dev、测试污染 dev | 测试配置错误                 | 测试必须使用 delivery_test        |
| IDEA 能连、Maven 不能连    | 地址或密码不同               | 对照环境变量和 JDBC URL           |
| Flyway 失败                | SQL、权限或脚本版本问题      | 保留完整错误，不删除迁移历史表    |

以后所有结构变化必须新增 Flyway 版本脚本，经过测试后提交。

### 2.4 C：验证前端工具

```powershell
node --version
npm --version
```

C 确认 Node/npm 可用、浏览器能打开本地地址，并将结果写入开发日志。

---

## 3. 第 0 天：Git 基线和文档归档

### 3.1 B 检查仓库基线

```powershell
git switch develop
git pull origin develop
git status --short
git log --oneline --decorate -8
```

确认 develop 包含后端骨架、main 是发布基线。不使用 git reset --hard，不覆盖其他成员未提交文件。

### 3.2 C 负责第一次文档归档

C 检查课程要求文档和 docs/dev-log/project-handover-2026-09-02.md 是否没有敏感信息，三人共同提交：

```powershell
git add 26271学期-软件工程综合实践.md docs/dev-log/project-handover-2026-09-02.md README.md docs
git commit -m "docs: archive course baseline and project handover"
git push origin develop
```

如果文件已经被提交，不要重复添加，改为确认远程分支一致。

---

## 4. 第 1 阶段第一步：冻结技术约定

时间目标：第 1 周前半段。B 主持，A 记录架构，C 更新项目说明。

三人开一次 30–60 分钟会议，逐项确认第 1 节内容。输出：

- A 更新 docs/architecture/README.md：模块边界、分层、JWT、统一异常和命名过渡；
- B 更新 docs/database/README.md：表、关系、约束、事务和逻辑删除；
- C 更新 README.md、frontend/README.md 和 docs/dev-log/README.md。

审查时必须回答：API、数据库和前端字段是否一致；店铺初始状态、取消规则和错误码是否固定；每个成员是否有可检查的输出物。

---

## 5. 数据库设计与 Flyway

负责人：B；审查人：A；前端字段检查：C。

### 5.1 数据关系

```text
users 1 ─── N orders
users 1 ─── N cart_items
users 1 ─── 0..1 merchants
merchants 1 ─── N shops
shops 1 ─── N product_categories
shops 1 ─── N products
products 1 ─── N cart_items
orders 1 ─── N order_items
products 1 ─── N order_items
```

### 5.2 8 张核心表最低字段

- users：id、account（唯一）、password_hash、nickname、phone、status、时间、deleted；
- merchants：id、user_id（唯一外键）、merchant_name、contact_phone、status、时间、deleted；
- shops：id、merchant_id、name、description、status、时间、deleted；
- product_categories：id、shop_id、name、sort_order、status、时间、deleted；
- products：id、shop_id、category_id、name、description、price、stock、status、时间、deleted；
- cart_items：id、user_id、product_id、quantity、时间，唯一约束 (user_id, product_id)；
- orders：id、order_number（唯一）、user_id、shop_id、total_amount、status、时间、cancelled_at、deleted；
- order_items：id、order_id、product_id、product_name_snapshot、unit_price_snapshot、quantity、subtotal。

所有外键、唯一约束、金额、库存和逻辑删除规则必须写入数据库文档。

### 5.3 B 的实施顺序

1. 先画实体关系；
2. 写数据库设计文档；
3. A 检查约束是否可测试；
4. C 检查前端字段是否足够；
5. 创建 backend/src/main/resources/db/migration/；
6. 编写 V1__create_core_tables.sql；
7. 编写开发/测试数据脚本；
8. 配置 Flyway；
9. 在 delivery_dev 和 delivery_test 分别迁移；
10. 验证外键、唯一约束和索引；
11. 保存日志；
12. 提交分支。

```powershell
git switch develop
git pull origin develop
git switch -c feature/b-database
git add docs/database backend/src/main/resources/db backend/src/main/resources/application*.properties
git commit -m "feat(database): add initial schema and Flyway migration"
git push -u origin feature/b-database
```

完成标准：

- 空数据库能完成迁移；
- 重复执行不会生成重复表；
- 账号、订单编号和购物车唯一约束有效；
- 库存不会减成负数；
- 历史订单保存名称、单价和数量快照；
- B 能解释每张表为何存在。

---

## 6. 后端公共架构

负责人：A；业务配合：B；契约确认：C。

### 6.1 模块内目录

不要建立全局大目录，保留 Modulith 模块：

```text
user/
  controller/
  service/
  mapper/
  entity/
  dto/
  exception/

merchant/
restaurant/       # 阶段 1 兼容旧名称
item/             # 阶段 1 兼容旧名称
shopping/
order/
```

### 6.2 A 必须先完成的公共组件

按顺序开发并先测试：

1. ApiResponse<T></t>：code、msg、data；
2. PageResult<T></t>：列表、页码、页大小、总数；
3. 统一错误码；
4. 业务异常基类；
5. 全局异常处理器；
6. Bean Validation；
7. JWT 生成和解析；
8. JWT 请求过滤器；
9. 当前用户上下文；
10. 日期时间和金额格式；
11. MockMvc 测试基类；
12. Modulith 模块边界测试。

A 必须自己确认异常映射、401/403 行为和敏感信息过滤，即使样板由 AI 生成。

完成标准：

- Controller 不包含跨资源业务；
- Service 负责规则、权限和事务；
- Mapper 只做数据库读写；
- Entity 不直接作为敏感响应；
- 所有错误和成功响应格式统一；
- 未登录返回 401，越权返回 403 或约定的 404；
- 公共组件测试提交早于功能代码。

---

## 7. REST 接口总契约

详细字段写入 docs/api/README.md。C 只能按该文档调用，B 只能按该文档实现。

### 7.1 固定接口

```text
POST   /api/v1/users
POST   /api/v1/users/login
GET    /api/v1/users/me
PATCH  /api/v1/users/me

POST   /api/v1/merchants

POST   /api/v1/shops
GET    /api/v1/shops
GET    /api/v1/shops/{shopId}
PATCH  /api/v1/shops/{shopId}

POST   /api/v1/shops/{shopId}/categories
GET    /api/v1/shops/{shopId}/categories
PATCH  /api/v1/categories/{categoryId}
DELETE /api/v1/categories/{categoryId}

GET    /api/v1/shops/{shopId}/products
GET    /api/v1/products/{productId}
POST   /api/v1/products
PATCH  /api/v1/products/{productId}

POST   /api/v1/cart-items
GET    /api/v1/cart-items
PATCH  /api/v1/cart-items/{itemId}
DELETE /api/v1/cart-items/{itemId}

POST   /api/v1/orders
GET    /api/v1/orders
GET    /api/v1/orders/{orderId}
POST   /api/v1/orders/{orderId}/cancel
```

每个接口文档必须包含方法、请求头、参数、成功响应、错误响应、权限、需求编号、测试编号和前端页面。

### 7.2 统一响应和状态码

```json
{
  "code": 0,
  "msg": "操作成功",
  "data": {}
}
```

- 200：查询或更新成功；
- 201：创建成功；
- 204：删除成功且无正文；
- 400：参数错误；
- 401：未认证或令牌无效；
- 403：已认证但无权限；
- 404：资源不存在；
- 409：重复资源、库存冲突或状态冲突；
- 500：未预期服务端错误。

列表使用 Query 参数 page、pageSize、keyword、status、sortBy、sortOrder。页码从 1 开始，未知排序字段必须拒绝或使用安全默认值，不能直接拼 SQL。

---

## 8. 阶段 1：按纵向切片实现功能

每个切片都执行：需求 → A 写测试 → Red → B 实现 → Green → 重构 → C 页面 → 联调 → 文档 → PR。

### 8.1 用户注册

对应 UC-USER-001、FR-USER-001。

A 在 feature/a-test-user 编写：

- 合法账号注册成功；
- 账号为空；
- 密码为空；
- 确认密码不一致；
- 密码长度或复杂度不合法；
- 账号重复返回 409；
- 数据库失败不产生残缺用户；
- 响应不包含密码。

A 运行并确认失败，记录到 docs/test/test-log.md：

```powershell
cd backend
.\mvnw.cmd -Dtest=*User*Test test
git add backend/src/test docs/test/test-log.md
git commit -m "test(user): add registration scenarios"
git push -u origin feature/a-test-user
```

B 在 feature/b-user 实现 DTO、Mapper、账号唯一查询、BCrypt、注册 Service、异常和 Controller。只写让测试通过所需的最少代码。通过后提交 feat(user): implement registration。

C 在 feature/c-user-pages 实现注册表单、确认密码、错误提示和 Axios 调用。三人联调一个合法账号和一个重复账号，确认数据库没有明文密码。

### 8.2 登录和个人信息

A 测试正确密码、错误密码、不存在账号、禁用账号、空参数、返回 JWT、无令牌访问 users/me 和越权。

B 实现登录查询、BCrypt 校验、JWT 生成、用户状态检查、个人资料查询和 PATCH。

C 实现登录页、Token 保存/清除、Axios 请求拦截、401 跳转和个人中心。个人中心不得显示密码。

### 8.3 商家和店铺

A 测试商家注册、未登录创建店铺、普通用户创建店铺、初始 CLOSED、非法状态、修改他人店铺、分页空结果和不存在店铺。

B 实现商家资料、店铺 Mapper/Service、归属校验、状态迁移、分页查询和逻辑删除。

C 实现商家注册、店铺管理、开关店、店铺列表、店铺详情、加载和空状态。

### 8.4 分类和商品

A 测试分类归属、分类删除限制、价格小于等于 0、库存小于 0、商品归属、商家越权、上下架、普通用户只看上架商品、详情不存在和历史价格快照。

B 实现分类和商品 Mapper、Service、校验、归属判断、上下架、库存调整、分页过滤和排序白名单。

C 实现分类管理、商品新增/编辑、库存调整、上下架、商品列表和详情，并显示缺货、下架状态。

### 8.5 购物车

A 测试未登录、商品不存在、商品下架、店铺闭店、数量非正数、超过库存、已有商品合并、越权修改/删除、重复删除和空购物车。

B 实现加入、合并、查询、修改、删除，并在每次操作时重新读取商品和店铺状态。

C 实现购物车列表、数量步进器、删除确认、空状态、缺货/下架/闭店提示和防重复点击。

### 8.6 订单

A 必须先提交以下测试：正常下单、未登录、空购物车、多店铺、闭店、下架、库存不足、整笔失败、服务端计算金额、快照、事务回滚、重复提交、订单越权、仅待支付可取消、取消不删除历史、已完成/已取消不可取消。

B 按以下顺序实现事务 Service：

```text
验证登录
→ 读取购物车
→ 检查购物车非空
→ 检查单一店铺
→ 检查店铺 OPEN
→ 检查商品 ON_SALE
→ 检查全部库存
→ 读取服务端价格
→ 计算金额
→ 保存订单
→ 保存订单明细快照
→ 扣减库存
→ 清理购物车
→ 提交事务
```

B 必须使用事务、库存条件更新、唯一订单号和统一状态枚举；失败抛业务异常以触发回滚。

C 实现订单确认、列表、详情和取消：显示服务端金额，提交按钮锁定，只有确认成功才提示成功，只有待支付订单显示取消。

订单现场演示必须包括：正常下单、库存减少、购物车清理、改价不影响历史订单、闭店拒绝、库存不足全回滚、取消只改状态。

---

## 9. 前端初始化和联调

C 在 feature/c-frontend-init 初始化 Vue 3 + Vite，安装 Vue Router、Pinia、Element Plus、Axios、Vitest。固定目录：

```text
frontend/src/
  api/
  components/
  router/
  stores/
  utils/
  views/
  tests/
```

Axios 统一处理 JWT 请求头、code、401 跳转、403/404/409 提示。页面不直接拼 URL，不重复写 Token 逻辑。

页面按注册、登录、个人中心、店铺、商品、商家管理、购物车、订单顺序开发。每页必须有加载、空数据、失败提示、表单校验和防重复提交。

C 至少测试登录状态、Token 清理、Axios 401、购物车数量边界、下单防重复、空列表、下架/缺货和订单取消按钮状态。

执行：

```powershell
cd frontend
npm install
npm run test
```

---

## 10. 每个功能的统一提交和 PR 流程

### 10.1 开始任务

```powershell
git switch develop
git pull origin develop
git switch -c feature/<任务名>
```

如果分支已存在，先确认是不是自己的未完任务，不要覆盖其他人的工作。

### 10.2 测试先行

A 提交测试前必须写明需求编号，覆盖正常、异常和边界场景，每个测试至少一个有效断言，运行并保存失败输出。

提交格式：

```text
test(<module>): add <behavior> scenarios
```

### 10.3 实现和重构

B 只能实现让失败测试通过所需的最少代码。测试通过后，A 检查覆盖率，B 删除重复代码，C 检查响应字段，再运行全量测试。

提交格式：

```text
feat(<module>): implement <behavior>
refactor(<module>): simplify <behavior>
```

### 10.4 PR 审查

PR 描述必须包含：做了什么、对应哪些用例、测试命令和结果、是否修改 API/数据库、是否需要前端联调、AI 用在哪里、已知缺陷。

审查人检查测试先于实现、分层、权限、异常、敏感信息和回归结果。合并到 develop 后删除远程 feature 分支，但保留提交历史。

---

## 11. 阶段 2：需求变更迭代

时间目标：第 2 周。具体变更以课程或小组实际收到的变更为准，不能提前虚构。

A 主持影响分析并先写测试；B 分析数据库、Mapper、Service 和事务；C 分析页面和交互。

每次变更必须：

1. 创建 CR 记录；
2. 写原始需求、新需求和原因；
3. 标记影响用例、规则、接口、表和页面；
4. A 写新测试并确认 Red；
5. B 在现有分层中实现；
6. C 更新前端；
7. 执行新增测试和全部旧测试；
8. 检查是否大面积修改无关模块；
9. 更新 API、架构、数据库、测试和开发日志；
10. PR 合并到 develop。

模板：

```markdown
# CR-编号：变更标题
- 提出日期：
- 原始需求：
- 新需求：
- 变更原因：
- 影响用例：
- 影响业务规则：
- 影响接口：
- 影响数据库：
- 新增/修改测试：
- 回归测试范围：
- 实现方式：
- 验收结果：
- 关联提交：
```

---

## 12. 阶段 3：重构、完整测试和交叉验收

### 12.1 A

运行后端全量测试、覆盖率、Modulith 边界、JWT、越权、异常、状态迁移和回滚测试，生成报告并归档。

### 12.2 B

在空数据库重新执行 Flyway；检查外键、唯一索引、逻辑删除、库存非负、订单快照、事务和幂等；清理重复 SQL。

### 12.3 C

完成页面美化、统一导航、错误提示、空状态、截图、演示材料和项目说明。

### 12.4 交叉验收

1. 选择另一个小组；
2. 提供运行说明、测试账号、演示数据和 API 文档；
3. 对方按用例执行；
4. 记录通过、失败和疑问；
5. 缺陷写入 docs/test/bug-list.md；
6. A 分配缺陷并负责回归；
7. B 修复后端/数据库；
8. C 修复前端/文档；
9. 交叉小组确认修复；
10. 归档验收报告。

交叉验收结论必须由学生判断，不能由 AI 代替。

---

## 13. 测试和缺陷记录

每条测试记录测试编号、用例/需求/规则编号、前置数据、步骤、预期和实际结果、有效断言、执行人、日期、命令、提交号和是否回归。

后端：

```powershell
cd backend
.\mvnw.cmd test
.\mvnw.cmd clean test
.\mvnw.cmd -Dtest=*Order*Test test
```

前端：

```powershell
cd frontend
npm run test
```

缺陷编号使用 BUG-001、BUG-002。缺陷必须有复现步骤、严重程度、责任人、修复提交和回归结果。

---

## 14. 常见问题处理顺序

1. 复制完整报错；
2. 记录执行命令和当前分支；
3. 检查是否刚刚合并新代码；
4. 查看 git diff 和 git status；
5. 先问负责该模块的人；
6. 使用 AI 解释报错和提出排查方向；
7. 人工验证修复方案；
8. 增加能复现问题的测试；
9. 修复后运行原测试和回归测试；
10. 在缺陷列表和开发日志中记录。

重点排查：Java 版本、Maven 依赖、MySQL 服务和端口、Flyway 脚本编号、JWT Bearer 头、401/403/404/409、前端代理和 API 字段名称。

---

## 15. 最终发布流程

### 15.1 发布前冻结

A 在 develop 执行：

```powershell
git switch develop
git pull origin develop
git status --short
```

三人确认：

- 工作区无未解释修改；
- 所有需求变更有记录；
- 所有测试通过；
- 覆盖率报告已生成；
- 空数据库可迁移；
- 前端可安装启动；
- 演示账号和数据可用；
- 没有密码、Token、.idea、target 或个人配置。

### 15.2 合并到 main

1. A 从 develop 发起发布 PR；
2. B 审查数据库、事务和后端；
3. C 审查页面、接口文档和验收材料；
4. 三人确认测试通过；
5. 合并到 main；
6. 创建标签 v1.0.0-course；
7. 在开发日志写入最终提交号；
8. 发布说明写明运行方式、分支、数据库迁移和已知限制。

### 15.3 演示顺序

普通用户注册/登录 → 商家注册/登录 → 创建并开店 → 创建分类和商品 → 用户浏览 → 加购物车 → 创建订单 → 查看详情 → 取消待支付订单 → 演示闭店、下架、库存不足 → 展示测试、覆盖率、Git 历史、需求变更和交叉验收。

---

## 16. 最终交付勾选表

### 代码和运行

- [ ] 后端能启动；
- [ ] 前端能安装和启动；
- [ ] 开发库和测试库能迁移；
- [ ] 用户、商家、店铺、商品、购物车、订单可演示；
- [ ] JWT、权限、事务和历史快照有效。

### 测试

- [ ] 所有核心接口有测试；
- [ ] 正常、异常、边界、权限、回滚和回归场景齐全；
- [ ] 核心接口覆盖率 100%；
- [ ] 关键业务方法覆盖率至少 90%；
- [ ] 前端关键交互有 Vitest；
- [ ] 最终全部测试通过。

### 文档

- [ ] 需求规格说明书；
- [ ] 项目说明；
- [ ] 架构设计；
- [ ] 数据库设计；
- [ ] Flyway 迁移说明；
- [ ] API 文档；
- [ ] 测试用例、日志和覆盖率；
- [ ] 缺陷、变更、回归和开发日志；
- [ ] AI 使用记录；
- [ ] 交叉验收报告；
- [ ] 发布说明。

### Git 和过程证据

- [ ] 每项任务有 feature 分支；
- [ ] 测试提交早于实现提交；
- [ ] 有 Red、Green、重构和回归证据；
- [ ] develop 已集成；
- [ ] main 已发布；
- [ ] 没有提交密码、Token、.idea、target 或个人配置；
- [ ] A、B、C 都能解释自己的接口、测试和业务规则。

---

## 17. AI 使用记录模板

每次使用 AI 后记录：

```markdown
| 日期 | 成员 | 场景 | AI 提供内容 | 人工修改/验证 | 关联提交 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-02 | A | 样板/排错/文档 | 简要说明 | 如何理解、修改并测试 | abc1234 |
```

允许 AI 做样板、排错和文档初稿；禁止 AI 决定模块边界、替代测试设计、替代业务判断和替代交叉验收。

---

## 18. 每天结束前检查

每个人每天结束前：

1. 执行 git status；
2. 检查敏感文件；
3. 运行负责范围测试；
4. 更新开发日志；
5. 写明下一步；
6. 记录具体阻塞错误；
7. 把需要其他成员接手的事项写进 PR 或日志。

三人共同确认 develop 没有未说明的红灯，接口文档和代码一致，每个完成功能都有测试和提交证据。

只要按照本手册执行，项目就能保持需求可追踪、测试可证明、代码可解释、变更可扩展、交付可验收。
+

---

# 附录 A：从空电脑开始的逐项操作

本附录假定三个人第一次接触 Git、Java、Spring Boot、Vue 和 MySQL。每一步都写出执行人、目录、命令、结果和交接内容。不要跳过“看起来很基础”的步骤，因为这些步骤会影响后续联调。

## A.1 第一次会议前：创建团队工作区

### A 先做

1. 在 GitHub 仓库中确认自己有 Write 权限；
2. 在仓库设置中确认默认分支是 main；
3. 确认已有 develop 分支；
4. 在 Issues 中建立以下任务：

```text
[基础] 环境和技术约定
[基础] 数据库和 Flyway
[基础] 后端公共架构
[用户] 注册、登录、个人信息
[商家] 商家注册和店铺
[商品] 分类和商品
[购物车] 购物车完整流程
[订单] 创建、查询、取消
[前端] 页面和联调
[测试] 覆盖率、回归和交叉验收
```

5. 为每个 Issue 写负责人和完成条件；
6. 将课程要求文档设为项目基线，不允许后续随意删改。

### B 先做

1. 检查本地 Git 是否指向正确 GitHub 仓库；
2. 检查 develop 是否比 main 多出后端骨架提交；
3. 记录当前最新提交号；
4. 记录本机 MySQL 版本、端口和数据库位置；
5. 不把 root 密码发到群里，使用私聊或本机配置。

### C 先做

1. 检查仓库中 frontend 是否只有 README；
2. 检查 Node.js 和 npm 版本；
3. 在 Issues 中建立“前端初始化”任务；
4. 记录浏览器版本；
5. 约定截图统一保存到 docs/acceptance/screenshots/。

## A.2 Windows 环境检查表

三人分别打开 PowerShell，逐条执行，复制输出保存到个人开发日志，不要只说“已经安装”。

```powershell
git --version
java -version
$env:JAVA_HOME
node --version
npm --version
mysql --version
```

完成标准：

| 工具  | 最低要求        | 不符合时                      |
| ----- | --------------- | ----------------------------- |
| Git   | 可执行 git 命令 | 安装 Git for Windows          |
| Java  | 17.x            | IDEA 和系统都切到 JDK 17      |
| Node  | 当前 LTS        | 安装 Node LTS，不使用过旧版本 |
| npm   | 随 Node 安装    | 删除错误的全局代理配置后重试  |
| MySQL | 8.x             | 检查 Windows 服务是否启动     |

如果 PowerShell 找不到命令：

1. 关闭当前终端；
2. 重新打开 PowerShell；
3. 检查系统 PATH；
4. 不要在项目中提交机器路径；
5. 仍失败时将完整错误发给负责该工具的人。

## A.3 IDEA 中的固定设置

A 负责写成截图说明，三人各自完成：

1. 打开 backend/pom.xml；
2. 选择 Project SDK = Java 17；
3. Maven Runner 使用 Java 17；
4. 打开 Annotation Processing；
5. 启用 Lombok；
6. 将文件编码设为 UTF-8；
7. 运行 BackendApplication；
8. 停止程序；
9. 运行全部测试；
10. 记录成功和失败结果。

B 额外确认：

- IDEA Database 工具可以连接 delivery_dev；
- IDEA Database 工具可以连接 delivery_test；
- 不勾选“保存密码到项目文件”；
- 不把数据库连接配置提交到 Git。

C 额外确认：

- IDEA 或 VS Code 可以打开 frontend；
- 终端默认目录能切换到 frontend；
- 浏览器开发者工具可以打开 Network 面板；
- 可以查看请求 URL、状态码和响应体。

## A.4 本机安全配置

所有人都遵守：

- 密码、JWT 密钥、数据库连接串不能出现在已跟踪文件；
- 不把 .env、application-local.properties、截图中的密码提交；
- 不把真实用户手机号写入测试报告；
- 不把 JWT Token 写入公开文档；
- 演示账号使用专门测试账号；
- 需要共享配置时只共享变量名和示例值。

本机配置变量名称统一为：

```text
DELIVERY_DB_URL
DELIVERY_DB_USERNAME
DELIVERY_DB_PASSWORD
DELIVERY_JWT_SECRET
```

文档中只能写：

```text
DELIVERY_DB_PASSWORD=<本机设置，不提交>
```

---

# 附录 B：每周每天的施工计划

## B.1 第 1 周：架构、数据库和基础闭环

### 第 1 天：基线和环境

A：

- 完成后端测试；
- 建立公共架构 Issue；
- 记录当前 Git 提交历史；
- 设计测试日志格式。

B：

- 建立 delivery_dev 和 delivery_test；
- 绘制数据库关系图；
- 决定字段、索引、外键和逻辑删除方式；
- 写数据库设计初稿。

C：

- 完成 Node/npm 检查；
- 初始化前端分支；
- 写前端目录约定；
- 准备接口调用记录模板。

当天结束必须有：

- 三人环境检查结果；
- 技术约定会议记录；
- 数据库 ER 图；
- API 命名约定；
- develop 上的基线提交。

### 第 2 天：公共后端和数据库迁移

A：

- 完成统一响应、错误码、异常处理测试；
- 完成 JWT 方案说明；
- 完成参数校验公共测试。

B：

- 完成 V1 数据库迁移；
- 在两个数据库执行；
- 创建基础测试数据；
- 完成 MyBatis 配置初稿。

C：

- 初始化 Vue/Vite；
- 安装 Router、Pinia、Element Plus、Axios、Vitest；
- 配置前端启动命令；
- 确认前端能显示基础页面。

当天结束必须有：

- A 的公共测试提交；
- B 的迁移脚本提交；
- C 的前端初始化提交；
- 三人各自的 feature 分支已经推送。

### 第 3 天：注册和登录

A：

- 先写注册和登录测试；
- 保存 Red 结果；
- 补充 JWT 过滤器测试。

B：

- 实现 users、merchants 的 Mapper 和 Service；
- 实现注册、登录和密码加密；
- 实现 JWT 生成。

C：

- 实现注册页、登录页；
- 完成 Axios Token 保存；
- 联调注册和登录。

### 第 4 天：个人信息、商家和店铺

A：

- 编写个人信息、商家注册、店铺权限测试；
- 编写状态迁移测试。

B：

- 实现 users/me、商家注册、店铺创建和状态更新；
- 实现店铺列表和详情。

C：

- 实现个人中心、店铺列表和商家管理页。

### 第 5 天：分类、商品和购物车

A：

- 写商品和购物车测试；
- 特别覆盖库存、下架和越权。

B：

- 实现分类、商品、购物车 Mapper 和 Service；
- 完成分页和排序白名单。

C：

- 实现商品浏览、商品管理和购物车。

### 第 6 天：订单核心

A：

- 先完成订单全套测试；
- 编写事务回滚测试；
- 编写幂等测试。

B：

- 实现订单创建、明细快照、库存扣减和购物车清理；
- 确保一个事务完成。

C：

- 实现订单确认、列表、详情和取消。

### 第 7 天：阶段 1 验收

三人共同：

1. 从干净数据库执行迁移；
2. 启动后端；
3. 启动前端；
4. 执行所有后端测试；
5. 执行所有前端测试；
6. 按完整业务链演示；
7. 记录缺陷；
8. 只把验收通过的分支合并到 develop；
9. 更新阶段 1 总结。

## B.2 第 2 周：需求变更

每天固定顺序：

1. A 记录变更；
2. B、C 做影响分析；
3. A 添加失败测试；
4. B 修改最少业务代码；
5. C 修改页面；
6. A 执行新增测试和回归；
7. 三人更新文档；
8. PR 合并 develop。

不要把“需求变更”写成一句口号。必须写出变更前后示例、影响字段和测试编号。

## B.3 第 3 周：收尾和交付

前 3 天：重构和缺陷修复。
中间 2 天：全量测试、覆盖率和交叉验收。
最后 2 天：材料检查、发布 PR、main 分支和演示。

---

# 附录 C：一个功能从开始到合并的逐命令流程

下面的流程以“新增用户注册”为例。其他功能只替换模块名和测试文件。

## C.1 A 创建测试分支

```powershell
git switch develop
git pull origin develop
git switch -c feature/a-test-user
```

A 做三件事：

1. 阅读需求中的 FR-USER-001、BR-USER-001、BR-USER-002；
2. 在测试文件中写出测试名称；
3. 不等待 B 写业务代码，直接运行测试。

测试名称建议：

```text
register_shouldCreateUserWhenInputIsValid
register_shouldRejectDuplicateAccount
register_shouldRejectBlankAccount
register_shouldRejectBlankPassword
register_shouldRejectMismatchedConfirmation
register_shouldNeverReturnPassword
```

运行：

```powershell
cd backend
.\mvnw.cmd -Dtest=UserRegistrationTest test
```

确认 Red 后，A 将输出写到：

```text
docs/test/test-log.md
```

日志至少写：

```text
日期：
分支：
测试命令：
预期：
实际失败数量：
失败原因：
下一步负责人：B
```

提交：

```powershell
git add backend/src/test docs/test/test-log.md
git commit -m "test(user): add registration scenarios"
git push -u origin feature/a-test-user
```

## C.2 B 接收测试并实现

B 先更新本地 develop，再创建分支：

```powershell
git switch develop
git pull origin develop
git switch -c feature/b-user
```

B 先阅读 A 的测试，不允许先写一套自己猜的实现。按下面顺序工作：

1. 定义注册请求 DTO；
2. 定义用户 Entity；
3. 创建 users Mapper；
4. 编写账号查询；
5. 编写密码摘要；
6. 编写注册 Service；
7. 把重复账号转成统一业务异常；
8. 编写 Controller；
9. 返回统一响应；
10. 运行 A 的测试；
11. 运行全部后端测试。

通过后提交：

```powershell
git add backend/src/main
git commit -m "feat(user): implement registration"
git push -u origin feature/b-user
```

## C.3 C 接收接口契约并实现

C 先从 develop 拉取包含 API 文档的版本：

```powershell
git switch develop
git pull origin develop
git switch -c feature/c-user-pages
```

C 按 API 文档完成：

1. 注册表单；
2. 前端必填校验；
3. 两次密码一致校验；
4. 调用 src/api/user.js 或等价文件；
5. 成功提示；
6. 409 重复账号提示；
7. 400 参数错误提示；
8. 网络失败提示；
9. 注册完成后跳转登录页；
10. 编写 Vitest。

## C.4 合并前检查

A 检查测试，B 检查业务，C 检查页面。三人都通过后：

```powershell
git switch develop
git pull origin develop
git merge --no-ff feature/a-test-user
git merge --no-ff feature/b-user
git merge --no-ff feature/c-user-pages
.\backend\mvnw.cmd -f backend\pom.xml test
cd frontend
npm run test
```

如项目设置要求通过 PR，则不要本地 merge，改为在 GitHub 发起三个 PR，目标统一为 develop。

---

# 附录 D：每个接口的详细任务卡

下面每张任务卡都必须复制到对应 Issue 中。A、B、C 完成后在 Issue 勾选，不能只在聊天中说“完成”。

## D.1 POST /api/v1/users：注册普通用户

### A：测试

- [ ] 合法账号返回 201；
- [ ] 账号为空返回 400；
- [ ] 密码为空返回 400；
- [ ] 确认密码不一致返回 400；
- [ ] 密码强度不足返回 400；
- [ ] 账号已存在返回 409；
- [ ] 数据库保存失败返回 500；
- [ ] 响应不含 password 或 password_hash；
- [ ] users 表新增一条 ACTIVE 记录。

### B：实现

建议文件位置：

```text
backend/src/main/java/com/delivery/backend/user/controller/UserController.java
backend/src/main/java/com/delivery/backend/user/service/UserService.java
backend/src/main/java/com/delivery/backend/user/mapper/UserMapper.java
backend/src/main/java/com/delivery/backend/user/entity/UserEntity.java
backend/src/main/java/com/delivery/backend/user/dto/RegisterRequest.java
backend/src/main/resources/mapper/user/UserMapper.xml
```

执行顺序：

1. 校验 DTO；
2. 查询 account；
3. 重复则抛冲突异常；
4. BCrypt 编码；
5. 插入用户；
6. 返回不含密码的 UserProfile；
7. 检查数据库异常是否回滚。

### C：实现

建议文件：

```text
frontend/src/views/RegisterView.vue
frontend/src/api/user.js
frontend/src/router/index.js
frontend/src/tests/register.spec.js
```

### 完成标准

A 的测试全绿，浏览器可注册，数据库没有明文密码，API 文档和测试日志已更新。

## D.2 POST /api/v1/users/login：登录

### A：测试

- [ ] 正确账号密码返回 200 和 JWT；
- [ ] 不存在账号返回登录失败；
- [ ] 密码错误返回登录失败；
- [ ] DISABLED 用户不能登录；
- [ ] 空账号或密码返回 400；
- [ ] Token 包含用户 ID 和角色；
- [ ] 密码不出现在响应和日志；
- [ ] 无 Token 访问受保护接口返回 401；
- [ ] 过期 Token 返回 401。

### B：实现

1. 查询 users；
2. 检查状态；
3. BCrypt 比较；
4. 读取是否存在 merchants 记录；
5. 生成 JWT；
6. 返回 userId、role、token；
7. 不把密码字段映射到响应 DTO。

### C：实现

1. 登录表单；
2. 登录成功保存 Token；
3. 保存当前用户和角色到 Pinia；
4. Axios 自动增加 Authorization；
5. 关闭浏览器后按约定处理登录状态；
6. 401 自动跳转；
7. 登录失败显示可理解提示。

## D.3 GET/PATCH /api/v1/users/me：个人信息

### A：测试

- [ ] 有效 Token 可查询本人；
- [ ] 无 Token 返回 401；
- [ ] 不能查询其他用户；
- [ ] PATCH 只允许 nickname/phone 等允许字段；
- [ ] 尝试修改 account 被拒绝；
- [ ] 非法手机号被拒绝；
- [ ] 更新失败不保存半条数据；
- [ ] 响应不含密码。

### B：实现

1. 从当前用户上下文取得 userId；
2. 根据 userId 查询；
3. 不接受 URL 中的用户 ID；
4. PATCH 只复制白名单字段；
5. 更新使用事务；
6. 返回安全 DTO。

### C：实现

1. 个人中心表单；
2. 读取当前用户；
3. 保存按钮防重复；
4. 成功后刷新 Pinia；
5. 显示校验和失败提示。

## D.4 POST /api/v1/merchants：商家注册

### A：测试

- [ ] 合法商家注册成功；
- [ ] 重复 account 失败；
- [ ] 商家名称为空失败；
- [ ] 联系方式非法失败；
- [ ] users 创建成功但 merchants 失败时整体回滚；
- [ ] 商家登录后角色正确。

### B：实现

1. 在事务中创建 users；
2. 创建 merchants 扩展记录；
3. 关联 user_id；
4. 设置 ACTIVE；
5. 失败时回滚 users；
6. 禁止重复 merchant 记录。

### C：实现

1. 商家注册页面；
2. 商家名称和联系方式；
3. 成功后跳转登录；
4. 显示重复账号和格式错误。

## D.5 POST /api/v1/shops：创建店铺

### A：测试

- [ ] 商家 Token 可创建；
- [ ] 普通用户返回 403；
- [ ] 无 Token 返回 401；
- [ ] 店铺名为空返回 400；
- [ ] 初始状态为 CLOSED；
- [ ] 店铺归属于当前 merchant；
- [ ] 数据库失败不生成半条店铺。

### B：实现

1. 从 Token 取得当前 userId；
2. 查询 merchant；
3. 检查 merchant ACTIVE；
4. 校验名称；
5. 插入 shop，状态固定 CLOSED；
6. 返回 shop DTO。

### C：实现

1. 商家店铺创建表单；
2. 初始状态显示“已关店”；
3. 成功后显示店铺详情；
4. 非商家显示无权限提示。

## D.6 GET/PATCH /api/v1/shops：店铺列表和状态

### A：测试

- [ ] 列表默认第一页 10 条；
- [ ] page=0 失败；
- [ ] pageSize 超上限失败；
- [ ] keyword 过滤；
- [ ] status 过滤；
- [ ] 无结果返回空列表；
- [ ] 商家只能 PATCH 自己的店铺；
- [ ] 非法状态失败；
- [ ] CLOSED/TEMPORARILY_CLOSED 不允许新订单；
- [ ] 状态修改不删除历史数据。

### B：实现

1. 分页参数统一校验；
2. sortBy 使用白名单；
3. 过滤条件参数化；
4. PATCH 前校验 merchant 归属；
5. 状态使用枚举；
6. 不删除历史订单和商品。

### C：实现

1. 用户店铺列表；
2. 关键字和状态筛选；
3. 分页组件；
4. 商家状态切换；
5. 状态标签颜色统一；
6. 空结果显示空状态。

## D.7 分类接口

### A：测试

- [ ] 商家只能操作自己店铺分类；
- [ ] 分类名称为空失败；
- [ ] 创建分类成功；
- [ ] 查询分类成功；
- [ ] 有商品引用的分类不能物理删除；
- [ ] 删除其他商家分类失败；
- [ ] 逻辑删除后用户不可见。

### B：实现

1. 校验 shop 属于 merchant；
2. 分类增删改查；
3. 删除前查询商品引用；
4. 有引用则拒绝或逻辑删除；
5. 查询过滤 deleted；
6. 返回排序后的分类。

### C：实现

1. 分类管理列表；
2. 新增、编辑、删除弹窗；
3. 有商品引用时显示后端错误；
4. 用户端按分类筛选。

## D.8 商品接口

### A：测试

- [ ] 价格必须大于 0；
- [ ] 库存必须大于等于 0；
- [ ] 分类和店铺必须存在；
- [ ] 商家只能操作自己的商品；
- [ ] 商品上架/下架成功；
- [ ] 下架商品不能加入购物车；
- [ ] 店铺商品列表支持分页；
- [ ] 商品详情和店铺不匹配时失败；
- [ ] 改价不影响历史订单。

### B：实现

1. 校验 shop、category 和 merchant；
2. 新增商品默认 OFF_SALE；
3. 修改字段使用 PATCH 白名单；
4. 库存更新使用非负校验；
5. 用户浏览只返回 ON_SALE；
6. 查询排序使用白名单；
7. 不修改 order_items 快照。

### C：实现

1. 商品新增和编辑；
2. 分类选择；
3. 价格和库存校验；
4. 上下架操作；
5. 用户商品列表；
6. 商品详情；
7. 缺货和下架状态展示。

## D.9 购物车接口

### A：测试

- [ ] 未登录返回 401；
- [ ] 商品不存在返回 404；
- [ ] 下架商品拒绝；
- [ ] 闭店拒绝；
- [ ] 数量小于 1 拒绝；
- [ ] 数量超过库存返回 409；
- [ ] 同商品再次加入时增加数量；
- [ ] 只能读取自己的购物车；
- [ ] 修改他人购物车返回 403/404；
- [ ] 删除不会扣库存；
- [ ] 空购物车返回成功空列表。

### B：实现

1. 取得当前 userId；
2. 读取 product 和 shop；
3. 检查状态和库存；
4. 查询是否已有 cart_item；
5. 已有则更新数量；
6. 没有则插入；
7. 修改数量重新检查库存；
8. 删除只影响 cart_items。

### C：实现

1. 购物车列表；
2. 商品名称、当前价格、小计；
3. 数量编辑；
4. 删除确认；
5. 失效商品提示；
6. 去结算按钮；
7. 结算期间锁定按钮。

## D.10 POST /api/v1/orders：创建订单

### A：测试

必须把每条业务规则写成一个独立测试，不要用一个大测试覆盖全部逻辑：

- [ ] 未登录；
- [ ] 购物车为空；
- [ ] 多店铺；
- [ ] 店铺 CLOSED；
- [ ] 店铺 TEMPORARILY_CLOSED；
- [ ] 商品 OFF_SALE；
- [ ] 商品不存在；
- [ ] 任一库存不足；
- [ ] 服务端价格计算；
- [ ] 客户端伪造金额被忽略；
- [ ] 明细保存名称快照；
- [ ] 明细保存单价快照；
- [ ] 订单创建后库存减少；
- [ ] 订单创建后购物车清空；
- [ ] 任一步失败全部回滚；
- [ ] 重复请求不产生重复订单；
- [ ] 订单状态为 PENDING_PAYMENT。

### B：实现

建议 Service 方法拆成可测试的小步骤：

```text
loadCart
validateSingleShop
validateShopOpen
loadProductSnapshots
validateProductsOnSale
validateAllStock
calculateServerTotal
createOrder
createOrderItems
decreaseStock
clearCart
```

B 必须保证外层事务包住完整流程，不能只给其中一个 Mapper 方法加事务。

### C：实现

1. 订单确认页只展示服务端重新计算后的价格；
2. 提交前再次确认；
3. 提交时禁用按钮；
4. 网络超时不显示成功；
5. 成功后根据订单号查询详情；
6. 失败返回购物车并提示原因。

## D.11 GET /api/v1/orders：订单列表

### A：测试

- [ ] 只返回当前用户订单；
- [ ] status 过滤；
- [ ] 默认创建时间倒序；
- [ ] 分页边界；
- [ ] 无订单返回空列表；
- [ ] 无 Token 返回 401。

### B：实现

1. 以当前 userId 作为强制条件；
2. 不接受客户端 userId；
3. status 使用枚举；
4. 排序字段白名单；
5. 返回分页对象。

### C：实现

1. 订单列表；
2. 状态筛选；
3. 分页；
4. 空状态；
5. 点击进入详情。

## D.12 GET /api/v1/orders/：订单详情

### A：测试

- [ ] 本人订单可查看；
- [ ] 他人订单不得泄漏；
- [ ] 不存在返回 404；
- [ ] 明细快照字段存在；
- [ ] 总金额与明细合计一致；
- [ ] 无 Token 返回 401。

### B：实现

1. 查询订单时同时校验 userId；
2. 组装明细；
3. 不读取商品当前价格覆盖快照；
4. 返回店铺、状态、总金额和明细。

### C：实现

1. 详情页；
2. 显示订单编号、店铺、状态、明细、金额；
3. 按状态显示取消按钮；
4. 不显示不属于当前用户的订单。

## D.13 POST /api/v1/orders//cancel：取消订单

### A：测试

- [ ] 本人待支付订单可取消；
- [ ] 他人订单不能取消；
- [ ] 不存在返回 404；
- [ ] PAID 不能取消；
- [ ] PREPARING 不能取消；
- [ ] COMPLETED 不能取消；
- [ ] 已取消不能再次取消；
- [ ] 订单记录保留；
- [ ] 取消时间写入；
- [ ] 并发取消只有一个成功。

### B：实现

1. 查询订单并校验归属；
2. 检查当前状态；
3. 只允许 PENDING_PAYMENT；
4. 使用条件更新避免并发覆盖；
5. 更新 CANCELLED 和 cancelled_at；
6. 按已经冻结的规则决定是否恢复库存；
7. 不删除订单和明细。

### C：实现

1. 取消按钮只在待支付显示；
2. 二次确认；
3. 成功后刷新状态；
4. 重复取消显示后端提示；
5. 失败不误显示成功。

---

# 附录 E：数据库和 MyBatis 的具体施工顺序

## E.1 B 创建数据库代码目录

```text
backend/src/main/resources/db/migration/
backend/src/main/resources/db/test-data/
backend/src/main/resources/mapper/user/
backend/src/main/resources/mapper/merchant/
backend/src/main/resources/mapper/shop/
backend/src/main/resources/mapper/product/
backend/src/main/resources/mapper/shopping/
backend/src/main/resources/mapper/order/
```

每个 Mapper XML 只负责对应表或明确的关联查询，不在 XML 中实现订单规则。

## E.2 迁移脚本编号规则

- V1__create_core_tables.sql：核心表；
- V2__add_indexes.sql：后续索引；
- V3__add_change_request_field.sql：阶段 2 需求变更；
- 不修改已执行的旧迁移脚本；
- 脚本一旦合并 develop，后续只能新增版本；
- 每个迁移脚本在空库和已有数据的库各执行一次。

## E.3 B 的数据库验证命令

```sql
SHOW TABLES;
DESCRIBE users;
DESCRIBE merchants;
DESCRIBE shops;
DESCRIBE product_categories;
DESCRIBE products;
DESCRIBE cart_items;
DESCRIBE orders;
DESCRIBE order_items;
SHOW CREATE TABLE orders;
SHOW CREATE TABLE cart_items;
```

验证项目：

- 外键存在；
- 唯一索引存在；
- status 字段值有限；
- price 精度足够；
- stock 不能为负；
- order_number 唯一；
- deleted 字段默认值正确；
- created_at 和 updated_at 会生成。

## E.4 订单事务人工验证

B 准备一条库存为 1 的商品，购物车数量设置为 2，调用创建订单接口。预期：

- 返回库存冲突；
- orders 没有新增记录；
- order_items 没有新增记录；
- products.stock 没有改变；
- cart_items 仍然存在。

B 把查询结果贴入测试日志，但隐藏账号和密码。

---

# 附录 F：AI/Agent 的安全使用方式

AI 可以帮助写样板，但每次提示都必须限定范围，避免一次生成整个系统。

## F.1 A 的测试提示模板

```text
你是测试辅助人员。请根据以下一个业务规则，给出测试场景建议，
不要生成完整测试代码，不要决定接口字段，不要决定架构。
输出正常、异常、边界、权限、回滚五类场景，并说明每类的预期 HTTP 状态码。
业务规则：<粘贴一个 BR 编号及内容>
```

A 看完建议后，自己选择场景、自己写测试、自己运行 Red。

## F.2 B 的业务实现提示模板

```text
你是 Spring Boot/MyBatis 代码助手。
只帮助我检查下面这个已经由小组决定的 Mapper 或 Service 片段。
不要重新设计接口，不要添加未讨论的业务规则，不要生成完整模块。
请指出编译错误、事务风险、SQL 注入风险和空值风险，并给出最小修改建议。
```

B 必须把 AI 建议改写成自己理解的代码，并让 A 的测试验证。

## F.3 C 的前端提示模板

```text
你是 Vue 3 页面样板助手。
根据以下已经确认的 API 契约，生成一个页面结构草稿。
不得更改请求字段、响应字段、状态码和业务规则。
只生成视图结构和交互占位，错误提示和权限判断由我人工补充。
API 契约：<粘贴 docs/api 中的一条接口>
```

## F.4 AI 输出验收

使用 AI 后，执行人必须回答：

1. 这段代码解决了哪个需求；
2. 它调用了哪个接口或 Mapper；
3. 它如何处理异常；
4. 它是否涉及权限；
5. 它是否需要事务；
6. 哪个测试能证明它正确；
7. 自己修改了 AI 输出什么；
8. 如果删除这段代码，系统哪个行为会变化。

答不出来就不能提交。

---

# 附录 G：接口联调操作剧本

联调工具统一选 Postman 或 ApiFox。以下以顺序为准，工具界面可以不同。

## G.1 准备环境

1. B 启动 MySQL；
2. B 执行 Flyway；
3. A 启动后端；
4. C 启动前端；
5. A 导入接口集合；
6. C 在浏览器 Network 面板观察同一请求；
7. 三人使用独立测试账号。

## G.2 用户和商家

1. POST 注册普通用户；
2. POST 登录普通用户；
3. 保存 JWT；
4. GET users/me；
5. POST 注册商家；
6. POST 登录商家；
7. 保存商家 JWT；
8. POST 创建店铺；
9. PATCH 店铺为 OPEN。

每一步记录：

- 请求；
- 状态码；
- 响应 code；
- 关键 data；
- 数据库变化；
- 测试编号。

## G.3 商品和购物车

1. 商家创建分类；
2. 商家创建商品；
3. 商品初始 OFF_SALE；
4. 商家 PATCH 为 ON_SALE；
5. 普通用户 GET 店铺商品；
6. 普通用户 POST 加购物车；
7. GET 购物车；
8. PATCH 数量；
9. DELETE 购物车项；
10. 再次加入，为订单演示准备。

## G.4 订单成功和失败

成功路径：

1. 店铺 OPEN；
2. 商品 ON_SALE；
3. 库存足够；
4. 购物车只有一个店铺；
5. POST orders；
6. 查询订单；
7. 检查库存和购物车。

失败路径逐个演示：

- 关闭店铺；
- 下架商品；
- 库存不足；
- 多店铺购物车；
- 无 Token；
- 重复取消；
- 查看他人订单。

## G.5 联调失败判断

- 状态码错误：A 对照 API 规范；
- code 错误：A 检查统一异常；
- 数据不变：B 检查事务和 Mapper；
- 页面不更新：C 检查响应拦截和 Pinia；
- 页面显示成功但后端失败：C 必须修复，不得用前端假成功掩盖；
- 接口字段不一致：三人暂停开发，先更新 API 文档再改代码。

---

# 附录 H：阶段验收会议议程

每次阶段验收由 A 主持，时长建议 60–90 分钟。

## H.1 会议前

A 准备：

- 测试通过截图；
- 覆盖率；
- Git 日志；
- 失败测试记录；
- 未解决缺陷。

B 准备：

- 数据库迁移结果；
- 表结构截图；
- 事务回滚结果；
- 测试数据说明。

C 准备：

- 页面演示顺序；
- 联调请求记录；
- 交互问题；
- 验收截图。

## H.2 会议中

按顺序回答：

1. 这次完成了哪些用例；
2. 哪条测试先于哪次实现；
3. 哪些异常场景被拒绝；
4. 数据库发生了什么变化；
5. 谁能说明订单事务；
6. 需求变化时修改了哪一层；
7. 前端如何处理 401、403、404、409；
8. 当前还剩哪些缺陷；
9. 是否达到阶段门槛。

## H.3 会议后

C 更新阶段总结；A 更新测试报告；B 更新数据库日志。三人共同决定是否允许合并到下一阶段。

---

# 附录 I：最终交付文件布局

最终仓库至少应接近以下结构：

```text
backend/
  src/main/java/
  src/main/resources/
    db/migration/
    mapper/
  src/test/java/
  src/test/resources/
frontend/
  src/
  package.json
  vite.config.*
e2e/
  README.md
  scenarios/
docs/
  api/README.md
  architecture/README.md
  database/README.md
  dev-log/README.md
  test/README.md
  test/test-cases.md
  test/test-log.md
  test/bug-list.md
  test/coverage/
  acceptance/
    screenshots/
    cross-acceptance-report.md
    release-checklist.md
README.md
26271学期-软件工程综合实践.md
从0到1-轻量级外卖平台-团队执行手册.md
```

每个目录都必须能说明“为什么存在”和“谁维护”。

---

# 附录 J：最终答辩准备

三人分别准备 3 分钟说明。

## A 需要能解释

- 为什么使用 Modulith；
- 为什么 Controller 不写业务；
- JWT 如何验证；
- 统一异常如何转换状态码；
- 一个测试如何证明权限；
- 一个测试如何证明事务回滚；
- Git 如何证明测试先于实现；
- 需求变更为什么没有破坏旧接口。

## B 需要能解释

- 8 张表的关系；
- 为什么 users 和 merchants 分开；
- 为什么订单明细保存快照；
- 为什么金额不能来自前端；
- 库存如何避免负数；
- 订单失败如何回滚；
- Mapper 和 Service 的边界；
- Flyway 为什么不能修改旧脚本。

## C 需要能解释

- 路由和页面如何组织；
- Pinia 保存什么；
- Axios 如何自动带 JWT；
- 401、403、404、409 如何显示；
- 如何防重复下单；
- 如何处理空列表和网络失败；
- 页面字段如何与 API 文档保持一致；
- 如何展示商品下架、缺货和店铺闭店。

答辩时可以打开代码，但不能只读代码。每个人都要先用自己的话解释，再用代码和测试证明。

---

# 附录 K：最后一天的逐小时清单

## 上午：冻结代码

- A：冻结 develop，运行后端全量测试；
- B：在空数据库执行 Flyway；
- C：运行前端构建和测试；
- 三人：停止新增功能，只修复阻塞缺陷。

## 中午前：冻结文档

- C：检查所有 README；
- A：检查测试日志、覆盖率、缺陷回归；
- B：检查数据库设计、迁移和测试数据；
- 三人：检查 AI 使用记录。

## 下午：交叉验收

- 执行完整演示；
- 记录每个结果；
- 修复阻塞问题；
- 重新运行相关测试；
- 更新验收报告。

## 晚上：发布

- A 发起 develop 到 main 的 PR；
- B 审查数据和事务；
- C 审查前端和材料；
- 合并 main；
- 创建版本标签；
- 下载或保存最终报告；
- 再次确认没有敏感文件；
- 把最终提交号写入开发日志。

---

# 附录 L：完整接口施工矩阵与 Agent 工单

这一张矩阵是三人实际分工的总表。开始某个接口前，先找到该行，再按照“测试 → 实现 → 页面 → 联调”的顺序执行。

| 接口                    | A 的测试文件/任务        | B 的后端实现位置                         | C 的页面/API 文件              | 测试分支               | 实现分支          |
| ----------------------- | ------------------------ | ---------------------------------------- | ------------------------------ | ---------------------- | ----------------- |
| POST users              | UserRegistrationTest     | user/controller + user/service           | RegisterView + api/user        | feature/a-test-user    | feature/b-user    |
| POST users/login        | UserLoginTest            | user/service + security                  | LoginView + auth store         | feature/a-test-user    | feature/b-user    |
| GET users/me            | UserProfileTest          | user/controller + service                | ProfileView + api/user         | feature/a-test-user    | feature/b-user    |
| PATCH users/me          | UserProfileUpdateTest    | user/service + mapper                    | ProfileView + api/user         | feature/a-test-user    | feature/b-user    |
| POST merchants          | MerchantRegistrationTest | merchant/controller + service            | MerchantRegisterView           | feature/a-test-shop    | feature/b-shop    |
| POST shops              | ShopCreateTest           | restaurant/shop controller + service     | MerchantShopView               | feature/a-test-shop    | feature/b-shop    |
| GET shops               | ShopListTest             | restaurant/shop mapper + service         | ShopListView                   | feature/a-test-shop    | feature/b-shop    |
| GET shops/{id}          | ShopDetailTest           | restaurant/shop service                  | ShopDetailView                 | feature/a-test-shop    | feature/b-shop    |
| PATCH shops/{id}        | ShopStatusTest           | restaurant/shop service                  | MerchantShopView               | feature/a-test-shop    | feature/b-shop    |
| POST categories         | CategoryCreateTest       | item/category controller + service       | CategoryManageView             | feature/a-test-product | feature/b-product |
| GET categories          | CategoryListTest         | item/category mapper                     | CategoryManageView/ProductView | feature/a-test-product | feature/b-product |
| PATCH categories/{id}   | CategoryUpdateTest       | item/category service                    | CategoryManageView             | feature/a-test-product | feature/b-product |
| DELETE categories/{id}  | CategoryDeleteTest       | item/category service                    | CategoryManageView             | feature/a-test-product | feature/b-product |
| GET shops/{id}/products | ProductListTest          | item/product mapper + service            | ProductListView                | feature/a-test-product | feature/b-product |
| GET products/{id}       | ProductDetailTest        | item/product service                     | ProductDetailView              | feature/a-test-product | feature/b-product |
| POST products           | ProductCreateTest        | item/product controller + service        | ProductManageView              | feature/a-test-product | feature/b-product |
| PATCH products/{id}     | ProductUpdateTest        | item/product service                     | ProductManageView              | feature/a-test-product | feature/b-product |
| POST cart-items         | CartAddTest              | shopping/controller + service            | CartView                       | feature/a-test-cart    | feature/b-cart    |
| GET cart-items          | CartListTest             | shopping/mapper + service                | CartView                       | feature/a-test-cart    | feature/b-cart    |
| PATCH cart-items/{id}   | CartUpdateTest           | shopping/service                         | CartView                       | feature/a-test-cart    | feature/b-cart    |
| DELETE cart-items/{id}  | CartDeleteTest           | shopping/service                         | CartView                       | feature/a-test-cart    | feature/b-cart    |
| POST orders             | OrderCreateTest          | order/controller + transactional service | CheckoutView                   | feature/a-test-order   | feature/b-order   |
| GET orders              | OrderListTest            | order/mapper + service                   | OrderListView                  | feature/a-test-order   | feature/b-order   |
| GET orders/{id}         | OrderDetailTest          | order/service                            | OrderDetailView                | feature/a-test-order   | feature/b-order   |
| POST orders/{id}/cancel | OrderCancelTest          | order/service                            | OrderDetailView                | feature/a-test-order   | feature/b-order   |

矩阵中“内部包名”保留现有 restaurant/item 是为了兼容当前骨架；接口、数据库和前端名称必须使用 shop/product。

## L.1 A 给测试 Agent 的单接口工单

A 每次只能给 Agent 一个接口，不能一次让 Agent 生成完整模块。提示内容固定如下：

```text
角色：测试设计助手。
项目：轻量级外卖服务平台。
当前接口：<方法和 URI>
需求编号：<FR 编号>
业务规则：<BR 编号和原文>
请只输出：
1. 正常场景；
2. 异常场景；
3. 边界场景；
4. 权限场景；
5. 数据库失败或事务场景（如果适用）；
6. 每个场景建议的 HTTP 状态码和断言。
禁止生成完整测试代码，禁止改变接口、数据库和业务规则。
```

A 看完 Agent 结果后必须自己：

1. 删除不适用场景；
2. 增加课程要求中没有被 Agent 提醒的场景；
3. 写测试；
4. 运行 Red；
5. 将失败证据提交。

## L.2 B 给实现 Agent 的单接口工单

B 必须先收到 A 的测试提交，然后才能使用下面的提示：

```text
角色：Spring Boot + MyBatis 实现检查助手。
当前接口：<方法和 URI>
已有测试：<测试类名和测试方法名>
已冻结规则：<业务规则>
请检查我已经写出的 DTO、Mapper、Service 或 Controller：
1. 是否有编译错误；
2. 是否违反分层；
3. 是否遗漏资源归属校验；
4. 是否可能信任客户端金额或状态；
5. 是否存在 SQL 注入或空值风险；
6. 是否需要事务。
不要重新设计 API，不要生成完整业务模块，不要增加未讨论的功能。
请给出最小修改建议。
```

B 使用 Agent 后必须：

1. 对每条建议逐项判断“采用/不采用”；
2. 在代码或开发日志中说明理由；
3. 运行 A 的测试；
4. 运行全量测试；
5. 通过后提交实现。

## L.3 C 给前端 Agent 的单接口工单

```text
角色：Vue 3 页面样板助手。
当前接口：<方法和 URI>
接口文档：<粘贴 docs/api/README.md 对应段落>
请生成页面结构草稿和 Axios 调用占位。
严格保持请求字段、响应字段、状态码和错误语义。
不要增加后端不存在的字段，不要修改路由规则，不要省略加载、空状态和错误状态。
```

C 必须人工补充：

- 表单校验；
- 401 跳转；
- 403/404/409 提示；
- 防重复提交；
- 空数据展示；
- Vitest；
- 可访问性和按钮禁用状态。

## L.4 每个接口的交接消息格式

A 完成测试后，必须发送：

```text
接口：
需求/规则：
测试提交：
已覆盖：
当前 Red 结果：
交给 B 的注意事项：
```

B 完成实现后，必须发送：

```text
接口：
实现提交：
修改的表/Mapper/Service/Controller：
事务情况：
测试结果：
交给 C 的响应示例：
```

C 完成页面后，必须发送：

```text
接口：
页面提交：
实际请求：
成功展示：
失败展示：
浏览器联调结果：
交给 A 的回归范围：
```

没有完成交接消息，下一名成员可以拒绝接手，避免“我以为你已经做完”的问题。

---

# 附录 M：统一请求、响应和错误字段

以下字段一旦写入 API 文档，后端和前端都不能自行改名。

## M.1 用户注册

请求：

```json
{
  "account": "user001",
  "password": "Password123",
  "confirmPassword": "Password123",
  "nickname": "小明",
  "phone": "13800000000"
}
```

成功 data：

```json
{
  "id": 1,
  "account": "user001",
  "nickname": "小明",
  "phone": "13800000000",
  "status": "ACTIVE"
}
```

禁止返回 password、passwordHash、password_hash。

## M.2 登录

请求：

```json
{
  "account": "user001",
  "password": "Password123"
}
```

成功 data：

```json
{
  "token": "<运行时生成，不写入文档>",
  "tokenType": "Bearer",
  "userId": 1,
  "role": "USER"
}
```

商家登录时 role 为 MERCHANT。错误时只返回“账号或密码错误”等可理解信息，不提示账号是否存在。

## M.3 店铺

创建请求：

```json
{
  "name": "小明汉堡店",
  "description": "自制汉堡"
}
```

成功 data：

```json
{
  "id": 1,
  "merchantId": 1,
  "name": "小明汉堡店",
  "description": "自制汉堡",
  "status": "CLOSED"
}
```

状态更新请求：

```json
{
  "status": "OPEN"
}
```

不允许前端传入 merchantId 作为权限依据，后端必须从 JWT 查询当前商家。

## M.4 分类

创建请求：

```json
{
  "shopId": 1,
  "name": "汉堡",
  "sortOrder": 1
}
```

如果路径中已有 shopId，也要以路径为准，不能信任请求体中的另一个 shopId。

## M.5 商品

创建请求：

```json
{
  "shopId": 1,
  "categoryId": 1,
  "name": "芝士汉堡",
  "description": "牛肉和芝士",
  "price": 18.50,
  "stock": 20
}
```

后端默认保存为 OFF_SALE，商家明确上架后才允许用户购买。

## M.6 购物车

加入请求：

```json
{
  "productId": 1,
  "quantity": 2
}
```

成功 data 至少包含：

```json
{
  "id": 1,
  "productId": 1,
  "productName": "芝士汉堡",
  "quantity": 2,
  "unitPrice": 18.50,
  "subtotal": 37.00,
  "status": "ON_SALE"
}
```

查询、修改和删除都只能依据当前 JWT 用户，不接受 userId 参数。

## M.7 订单

创建请求：

```json
{
  "idempotencyKey": "client-generated-unique-value"
}
```

本版本不接受客户端 totalAmount。订单商品从当前用户购物车读取。

成功 data 至少包含：

```json
{
  "id": 1,
  "orderNumber": "ORD202609020001",
  "shopId": 1,
  "totalAmount": 37.00,
  "status": "PENDING_PAYMENT",
  "items": [
    {
      "productId": 1,
      "productNameSnapshot": "芝士汉堡",
      "unitPriceSnapshot": 18.50,
      "quantity": 2,
      "subtotal": 37.00
    }
  ]
}
```

取消订单不需要删除请求体，调用：

```text
POST /api/v1/orders/{orderId}/cancel
```

## M.8 分页

所有分页列表至少返回：

```json
{
  "items": [],
  "page": 1,
  "pageSize": 10,
  "total": 0,
  "totalPages": 0
}
```

页码错误、页大小错误和未知排序字段必须在后端拒绝。

## M.9 错误响应

统一格式：

```json
{
  "code": 40001,
  "msg": "请求参数不合法",
  "data": null
}
```

建议错误码分组：

| 范围  | 含义                 |
| ----- | -------------------- |
| 400xx | 参数错误             |
| 401xx | 未登录或令牌无效     |
| 403xx | 权限不足             |
| 404xx | 资源不存在           |
| 409xx | 重复、库存或状态冲突 |
| 500xx | 服务端未预期错误     |

错误码一旦在 API 文档中公布，后端、前端和测试必须共用。

---

# 附录 N：每次提交前的逐项人工操作

## N.1 A 提交前

```powershell
git status --short
git diff -- backend/src/test docs/test
cd backend
.\mvnw.cmd test
```

A 逐项确认：

- [ ] 测试名称能说明行为；
- [ ] 测试有有效断言；
- [ ] 测试先于业务实现；
- [ ] Red 记录已保存；
- [ ] Green/回归结果已保存；
- [ ] 没有把业务实现混进测试提交；
- [ ] 覆盖率变化可以解释。

## N.2 B 提交前

```powershell
git status --short
git diff -- backend/src/main backend/src/main/resources
cd backend
.\mvnw.cmd test
```

B 逐项确认：

- [ ] Mapper 只做数据访问；
- [ ] Service 包含业务规则；
- [ ] Controller 没有复杂流程；
- [ ] 所有资源归属由服务端检查；
- [ ] 金额来自数据库；
- [ ] 库存更新有条件；
- [ ] 事务范围正确；
- [ ] 密码和 Token 没有进入日志；
- [ ] SQL 使用参数，不拼接用户输入；
- [ ] 数据库迁移脚本没有修改旧版本。

## N.3 C 提交前

```powershell
git status --short
git diff -- frontend
cd frontend
npm run test
npm run build
```

C 逐项确认：

- [ ] 页面只调用 src/api 封装；
- [ ] 字段和 API 文档一致；
- [ ] 401/403/404/409 有提示；
- [ ] 列表有加载和空状态；
- [ ] 下单按钮会防重复；
- [ ] 错误时没有假成功；
- [ ] Token 没有写死；
- [ ] 没有提交 .env 或个人配置；
- [ ] 页面关键交互有 Vitest。

## N.4 PR 合并前

审查成员在 GitHub PR 中逐条评论：

```text
[ ] 需求编号正确
[ ] 测试先于实现
[ ] 正常/异常/边界/权限已覆盖
[ ] API 文档已更新
[ ] 数据库文档或迁移已更新
[ ] 前端联调范围明确
[ ] 全量测试通过
[ ] 没有敏感信息
[ ] 没有无关文件
[ ] 代码可以由提交人解释
```

所有复选框完成后才能点 Merge。

---

# 附录 O：验收用测试账号和数据

测试账号不要使用真实个人信息，建议建立以下固定演示数据：

| 账号            | 类型     | 用途                 |
| --------------- | -------- | -------------------- |
| demo_user       | 普通用户 | 浏览、购物车、下单   |
| demo_merchant   | 商家     | 店铺、分类、商品管理 |
| demo_user_2     | 普通用户 | 权限越权测试         |
| demo_merchant_2 | 商家     | 商家资源隔离测试     |

演示数据：

- 一个 OPEN 店铺；
- 一个 CLOSED 店铺；
- 一个 TEMPORARILY_CLOSED 店铺；
- 一个 ON_SALE 且库存大于 0 的商品；
- 一个 ON_SALE 且库存为 0 的商品；
- 一个 OFF_SALE 商品；
- 一个已有商品的分类；
- 一个待支付订单；
- 一个已完成订单；
- 一个已取消订单。

初始化演示数据后，B 在开发日志记录数据用途，不能记录密码和 Token。

---

# 附录 P：项目真正“结束”的判定

项目不是“能打开首页”就结束，必须同时满足以下条件：

1. 从空数据库可以迁移；
2. 后端和前端可以按 README 启动；
3. 所有核心接口可通过；
4. 失败场景会返回正确状态码；
5. 普通用户和商家权限隔离；
6. 订单事务和库存一致性有测试证据；
7. 商品改价不影响历史订单；
8. 需求变更有新增测试和回归记录；
9. 核心接口覆盖率达到 100%；
10. 关键业务方法覆盖率达到 90%；
11. Git 历史能证明测试先于实现；
12. 交叉小组可以独立运行和验收；
13. A、B、C 可以分别解释自己的工作；
14. 所有文档和截图已归档；
15. main 分支和版本标签已经建立。

只有全部打勾，才可以把项目状态写成“已完成并交付”。
