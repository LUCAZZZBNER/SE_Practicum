CREATE TABLE IF NOT EXISTS images (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT, url VARCHAR(500) NOT NULL, content_type VARCHAR(100) NOT NULL, size BIGINT UNSIGNED NOT NULL, content LONGBLOB NULL, created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3), PRIMARY KEY(id));
