# Backend

Spring Boot 后端项目目录。

- `src/main/java/`：Entity、Mapper、Service、Controller 等业务代码
- `src/main/resources/`：应用配置、数据库迁移脚本等资源
- `src/test/java/`：JUnit 5、Mockito、MockMvc 测试
- `src/test/resources/`：测试环境配置和测试数据

后端工程初始化后，在此目录执行 `mvn test`。
