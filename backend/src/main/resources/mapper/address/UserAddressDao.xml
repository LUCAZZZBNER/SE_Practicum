<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.delivery.backend.address.dao.UserAddressDao">
  <resultMap id="address" type="com.delivery.backend.address.entity.UserAddressEntity">
    <id property="id" column="id"/><result property="userId" column="user_id"/><result property="recipient" column="recipient"/>
    <result property="phone" column="phone"/><result property="region" column="region"/><result property="detail" column="detail"/>
    <result property="defaultAddress" column="is_default"/><result property="createdAt" column="created_at"/><result property="updatedAt" column="updated_at"/><result property="deletedAt" column="deleted_at"/>
  </resultMap>
  <select id="list" resultMap="address">SELECT id,user_id,recipient,phone,region,detail,is_default,created_at,updated_at,deleted_at FROM user_addresses WHERE user_id=#{userId} AND deleted_at IS NULL ORDER BY is_default DESC,id ASC</select>
  <select id="findOwned" resultMap="address">SELECT id,user_id,recipient,phone,region,detail,is_default,created_at,updated_at,deleted_at FROM user_addresses WHERE user_id=#{userId} AND id=#{id} AND deleted_at IS NULL</select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO user_addresses(user_id,recipient,phone,region,detail,is_default) VALUES(#{userId},#{recipient},#{phone},#{region},#{detail},#{defaultAddress})</insert>
  <update id="clearDefaults">UPDATE user_addresses SET is_default=FALSE WHERE user_id=#{userId} AND deleted_at IS NULL</update>
  <update id="update">UPDATE user_addresses <set><if test="recipientSpecified">recipient=#{recipient},</if><if test="phoneSpecified">phone=#{phone},</if><if test="regionSpecified">region=#{region},</if><if test="detailSpecified">detail=#{detail},</if><if test="defaultSpecified">is_default=#{defaultAddress},</if></set> WHERE id=#{id} AND user_id=#{userId} AND deleted_at IS NULL</update>
  <update id="softDelete">UPDATE user_addresses SET deleted_at=CURRENT_TIMESTAMP(3) WHERE id=#{id} AND user_id=#{userId} AND deleted_at IS NULL</update>
</mapper>
